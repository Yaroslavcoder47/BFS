<h1>BFS algorithm</h1>
<p>Here is presented a breadth-first search algorithm for an undirected graph written through an adjacency list. Ultimately, an array is output with the distances from the starting vertex (1) to all vertices in the graph. Moreover you can read an article on the theme of BFS algorithm that I wrote.</p>
